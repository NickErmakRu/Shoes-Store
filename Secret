Secret


SELECT battle.name as battle_name, heroes.name as hero FROM battle 
	JOIN participants ON participants.id_battle = battle.id 
	JOIN heroes ON heroes.id = participants.id_hero 
	WHERE battle.name = "Альтрон"